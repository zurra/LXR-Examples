# LXR-Examples

Examples for the LXR Light Detection plugin for Unreal 5.5.

Memory example is currently broken because of some weirdness with the scene capture, which I use to render the minimap for the Memory example.

